# A minimal nirs4all pipeline (fully JSON/YAML-serializable — safe to send over
# the network as a 'path', 'artifact' or 'inline_json' pipeline reference).
pipeline:
  - class: sklearn.preprocessing.StandardScaler
  - class: sklearn.cross_decomposition.PLSRegression
    params:
      n_components: 5
